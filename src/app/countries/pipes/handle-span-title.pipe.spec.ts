import { HandleSpanTitlePipe } from './handle-span-title.pipe';

describe('HandleSpanTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new HandleSpanTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
